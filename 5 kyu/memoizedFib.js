var fibonacci = function(n, arrayOfSolutions) {
    if (typeof array === 'undefined') {
      let arrayOfSolutions = []
    } 
    if(n===0 || n === 1)
        return n;
    const findIfKeyExistsInArr = (arr, key) => (arr.filter(x => x.key === key).length > 0)
    if (arrayOfSolutions.filter(x => x.key === n).length === 1) {
      
    }
    
    //return fibonacci(n-1) + fibonacci(n-2);
}